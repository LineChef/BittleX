# Pi pipeline — core dependencies (Phase 7+). Pure-Python, install anywhere.
# Audio backends (Vosk / Piper / sounddevice) are in requirements-audio.txt so
# the text-mode pipeline can run without them.
#
#   python3.11 -m venv pi_pipeline/.venv
#   source pi_pipeline/.venv/bin/activate
#   pip install -r pi_pipeline/requirements.txt
#
# Pinned to the versions running green on the dev Mac. All pure-Python, so the
# Pi install is wheel-only. anthropic is pinned because its API surface shifts
# between minor versions.

anthropic==1.2.0
python-dotenv>=1.0,<2
pyserial==3.5
